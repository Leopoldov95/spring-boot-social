import React from "react";
import Navbar from "../components/global/Navbar";

const Explore = () => {
  return (
    <div>
      {/* Search Bar */}
      {/* Mode View Setter [posts, accounts, tags] */}
      {/* some kind of dropwon, one tag at a time */}
      {/* All Posts view */}
      {/* All accounts view */}
      {/* All Posts by tags */}
      {/* Navbar */}
    </div>
  );
};

export default Explore;
