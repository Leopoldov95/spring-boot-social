import React from "react";
import Navbar from "../components/global/Navbar";

// Can only be accessed by auth user
const Settings = () => {
  return (
    <div>
      {/* Edit names */}
      {/* Edit Email */}
      {/* Set Theme */}
      {/* About Us */}
      {/* Logout */}
      {/* Navbar */}
    </div>
  );
};

export default Settings;
