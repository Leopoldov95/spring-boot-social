import React from "react";
import Navbar from "../components/global/Navbar";

const Profile = () => {
  return (
    <div>
      {/* If auth user then settings */}
      {/* User vs Other User's profile */}
      {/* Profile details (avatar) tagline */}
      {/* Follower details and num posts */}
      {/* User posts */}
      {/* Navbar */}
    </div>
  );
};

export default Profile;
